{
  "name": "onlinemodelcost",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync:prices": "node scripts/sync-openrouter-prices.mjs",
    "deploy": "npm run build && wrangler pages deploy ./dist --project-name onlinemodelcost --branch main --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^7.2.9",
    "gpt-tokenizer": "^4.0.0"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
